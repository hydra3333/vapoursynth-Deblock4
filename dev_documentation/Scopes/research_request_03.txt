Research question: free tools to identify MPEG-2 grid/DCT and interlacing
characteristics from a source clip, per frame

I have MPEG-2 program-stream .mpg video files (PAL, standard-definition, 4:2:0),
which may be progressive, interlaced top-field-first, or interlaced
bottom-field-first. For a video-restoration/deblocking project I need to
characterise a source clip precisely - by inspecting the first N frames
(default 2) - so that when I test my deblocking software I know exactly what I
am dealing with and what results to expect.

I already use MediaInfo and ffprobe and know they report container- and
stream-level facts. Sample of what I already get (MediaInfo):

    Complete name                   : D:\VHS\home_576i.mpg
    Format                          : MPEG-PS
    File size                       : 1.30 GiB
    Duration                        : 34 min 1 s
    Overall bit rate mode           : Constant
    Frame rate                      : 25.000 FPS
    Video
    ID                              : 224 (0xE0)
    Format                          : MPEG Video
    Format version                  : Version 2
    Format profile                  : Main@Main
    Format settings                 : CustomMatrix / BVOP
    Format settings, BVOP           : Yes
    Format settings, Matrix         : Custom
    Format settings, GOP            : M=3, N=24
    Format settings, picture structure : Frame
    Bit rate mode                   : Constant
    Bit rate                        : 5 200 kb/s
    Width                           : 720 pixels
    Height                          : 576 pixels
    Display aspect ratio            : 16:9
    Frame rate                      : 25.000 FPS
    Standard                        : PAL
    Color space                     : YUV
    Chroma subsampling              : 4:2:0
    Bit depth                       : 8 bits
    Scan type                       : Interlaced
    Scan order                      : Top Field First
    GOP, Open/Closed                : Open

I need a DEFINITIVE answer - not "maybe" or "possibly" - on tool availability
for three tiers of information, because they differ in difficulty and I suspect
they have different answers.

TIER 1 - stream/sequence-level (I believe this is already solved; confirm which
tool prints each):
    progressive vs interlaced; field order (TFF/BFF); frame vs field picture
    structure; resolution; chroma subsampling; aspect ratio.

TIER 2 - per-picture, for each of the first N frames:
    picture coding type (I/P/B); picture structure; the progressive_frame,
    top_field_first, and repeat_first_field flags.
    Which free tool prints these per frame, and in what exact command form?

TIER 3 - per-macroblock DCT type (the one I most need a definitive answer on):
    MPEG-2 chooses, per macroblock, between frame-based and field-based DCT
    (the dct_type bitstream element). I want to know, per frame, whether
    frame-DCT or field-DCT macroblocks are present and in what proportion.
    Is there any free tool that decodes and reports MPEG-2 per-macroblock
    dct_type (frame vs field DCT) in a labelled and ideally aggregatable form?
    I have found that "ffmpeg -debug mb_type" reports macroblock PREDICTION
    type (intra / forward / backward), NOT dct_type, so that does not answer it.
    If the answer is "no standard tool exposes this", say so plainly - that is
    a useful and acceptable answer. If a tool does, I need the exact tool,
    version, and command, and confirmation it reports dct_type specifically
    rather than some other macroblock attribute.

SEPARATELY - an alternative I am also considering:
    Rather than reading the bitstream's dct_type, I could measure the actual
    block-edge grid present in the DECODED PIXELS (e.g. separate fields, then
    measure vertical block-edge periodicity to see whether edges fall on a
    4-line or 8-line pitch). Is there a free tool that measures decoded-pixel
    block-grid periodicity / blockiness pitch, or is this something one
    normally implements oneself (e.g. via a VapourSynth or AviSynth script or a
    small image-analysis program)? A definitive "no standard tool; roll your
    own" is an acceptable answer if that is the reality.

HOW TO ANSWER:
    - For every tool you suggest, give a DEFINITIVE statement: the exact tool,
      its version, the exact command line, and precisely which of the three
      tiers (and which specific fields) it reports. No "it might" or "possibly"
      - if you are not certain a tool reports a given field, say you are not
      certain rather than implying it does.
    - Distinguish clearly between what you can state from documentation or
      established use versus your own inference.
    - If a tier has no free tool that reports it, say so plainly - knowing a
      capability does not exist is as useful as finding one.
    - Free tools only (open-source or free-to-use). Windows-runnable preferred;
      command-line acceptable and preferred.
