Designer thinking
-----------------

Let me review the delivery thoroughly against scope v1.3. First, extract and inventory everything, then work my seven-point review list, verifying against raw artifacts rather than trusting the coder's own report.

Extract and inventory the delivery
Extract and inventory the delivery

All expected files present. Let me establish the review baseline — first check the coder's own report and static validation claims, then verify independently. Starting with the two files most likely to hide problems: the config module (must be declarations-only) and the detection core (the load-bearing one).

Review config module for declarations-only compliance
Review config module for declarations-only compliance

Config module is clean — declarations-only, correct namespaces, deblock4_build_options import name, version_string present, CRLF. Matches scope §2.2 exactly. Now the detection core — the load-bearing file. Let me read it in sections.

Read detection core head: records, enums, asm
Read detection core head: records, enums, asm

Excellent structure. The C-3 pattern is correctly used (both gated imports, else struct {}), the enums match the pinned §5A contract, provenance enum present (B7), all 25 features. Let me continue — the asm, the detection logic, and the membership cross-check (the load-bearing §3.6).

Read asm wrappers and detection logic
Read asm wrappers and detection logic

Strong work. The once-mechanism is a hand-rolled atomic CAS (defensible — the coder's stated latitude), the intersection is genuinely structural (every effective bit is allowed AND isPresent(actual) — cannot fabricate), and there's a nice detail: an object-mode semantic root (standalone_mode_root) so the detection object emits for the §7.2 disassembly proof. Let me see the asm, detection, membership cross-check, and the reason logic.

Read reason logic, tier helpers, and asm
Read reason logic, tier helpers, and asm

Detection logic is correct: every bit matches the §1.2 table (I'm cross-checking each), leaf guards present, lahf_sahf/lzcnt correctly guarded on the extended leaf, OSFXSR/SCE hardcoded policy_assumed_present, XCR0 only queried when OSXSAVE present, whole-level v2⊆v3 dependency correct. The reason logic matches the deterministic rule (force-down precedence, next-level-up missing list, XCR0.YMM pseudo-feature, table order). Now the asm and the membership cross-check — the two remaining load-bearing pieces.

Read asm wrappers and membership cross-check
Read asm wrappers and membership cross-check

This is the crux — the membership cross-check, implemented properly. The asm matches the reference shape with the [_] anonymous-name form (a valid Zig 0.16 spelling), EBX declared as output with the Win64 comment, noinline so the object emits. The classifyModelFeature covers exactly the seed map + exclusions from scope §3.6. And crucially, the else => null -> @compileError on any unclassified feature is the fail-closed behavior. Let me see the rest of the verification (the membership equality check itself) and then the print/gated modules.

Read the membership equality assertion body
Read the membership equality assertion body

This exceeds the scope's ask in a good way. The coder implemented two cross-checks: (1) verifyNamedModelMembership — every model feature classified, membership matches the level's set-differences, @compileError on drift or unclassified; and (2) verifyCapturedModelSet — pins the exact feature set of each named model against the 1B.2 captures, so a Zig upgrade that adds/removes any member (even a tuning flag) fails loudly. That second one is a stronger drift-tripwire than §3.6 required. The else => null -> @compileError is fail-closed. Let me quickly verify the print modules, gated modules, selftest, build.zig, and then check the coder's own validation claims and the FPU note (I notice FPU isn't in the classify map — need to check that's handled).

Check FPU/policy-row handling and read print helper
Check FPU/policy-row handling and read print helper

FPU is correctly handled — it's detected from CPUID (bitState(leaf_1.edx, 0)) in our record, and on the Zig-model side maps from .x87 (the comment says so). OSFXSR/SCE are policy rows, never expected in Zig models — correctly absent from classifyModelFeature. The summary line format matches §4.1 exactly. Now let me check the two gated modules and selftest, then run the coder's static-validation claims and check what the coder says it could NOT do (since W3X runs builds).

Read the gated force-down module
Read the gated force-down module

This module is very well done. C-3 pattern, non-allocating GetEnvironmentVariableW into a fixed [8]u16 buffer, exact v1/v2 match, empty->invalid, overlong->invalid, not-found->null, other error->@panic — matching §5.1/R4 precisely. Unique string marker AND code-immediate marker (0xFD00_D001) with a retained probe export for the three-surface proof. Ceiling correctly has no v3 value. Let me quickly check the diag module, selftest, build.zig, dll_probe, then the coder's own validation status and report.

Read selftest and dll_probe
Read selftest and dll_probe

Both good. Selftest exercises once-semantics (pointer identity), the v3-host positive case, and requested-backend-doesn't-alter-capability. dll_probe has the minimal addition (calls initInstanceCapabilities, keeps CRLF... wait — dll_probe is LF while everything else is CRLF; let me note that). Now the critical pieces I haven't verified: the §7.1 clamp unit tests (where are they?), build.zig, the batch, and the coder's own status claims. Let me check.

Locate the clamp unit tests
Locate the clamp unit tests

The clamp tests are exactly the B3 ask — exhaustive nine-combination actual × ceiling, verifying effective never exceeds actual, the intersection property per feature, and the at-or-above-loud case. Pure functions over fabricated records, no synthetic seam in production. Now the build.zig (the reject checks + all the artifacts) and the coder's validation status + report claims. Let me check those and the manifest/static-validation.

Ran 2 commands
Ran 2 commands

build.zig is complete and correct: options + reject (@panic unless Debug), all artifacts (DLL, selftest with selftest/selftest-run steps, detection-object at baseline-v1 target installed to detection-objects/, test step wiring the capability tests), and deblock4_build_options wired into every module that needs it. The detection object uses baseline_target (v1) — let me confirm that one line, then check the coder's own validation status (what it could/couldn't run) and the batch. That's the key remaining question: did the coder build it, or is this static-only?

Confirm detection object target and read coder's validation status
Confirm detection object target and read coder's validation status

Now I have the key context: this is a static-only delivery — the coder explicitly states Zig compilation, Windows execution, and dumpbin were unavailable in its environment. So the delivery is structurally complete and statically validated, but not yet built or run. That's the crucial thing the new coder + W3X must close. Let me verify the diag module and the batch's proof structure quickly, confirm the dll_probe LF anomaly, then write the review.

Check diag module and batch proof structure
Check diag module and batch proof structure

The batch is comprehensive — it covers the full §7 matrix: 1B.2 regression, ReleaseSafe/ReleaseFast production builds with three-surface absence scans on both artifacts, the Debug positive control with markers present, force-down behaviour matrix (absent/v1/v2/invalid), and the non-Debug reject checks. The diag module correctly renders provenance ("OS baseline assumed..."). Everything is structurally coherent and mutually consistent. Let me verify the SHA manifest integrity and the dll_probe LF question, then I have enough to write the review.

Ran 2 commands
Ran 2 commands

Confirmed — the original dll_probe.zig in the repo is LF, so the coder correctly preserved the existing file's line endings (charter C-DELIV-04: replacement isn't permission for unrelated cleanup). New files are CRLF, the modified existing file keeps its LF. That's a point of correctness, not a defect. My review basis is complete. Writing it up.

Final checks for writeup
Final checks for writeup

The one unreachable is provably safe (the ceiling local is built from a switch over Ceiling{v1,v2} only, so .x86_64_v3 genuinely can't occur). I have everything. Writing the review document.

Created a file, ran a command, read a file
Created a file, ran a command, read a file

Review delivered: Deblock4_Stage_1B3_W3D_Delivery_Review_v1_0.md — ready to hand to the new coder alongside the scope and the delivery zip.

The verdict, in short

Design-complete, scope-faithful, no blocking defect — but static-only, so nothing is proven yet. The prior coder's environment couldn't run Zig, execute on Windows, or run dumpbin, so the entire §7 build/run/disassembly/absence-proof set is still owed. That's the one crucial thing for the new coder and W3X to close — and it's closure work, not redesign.

What I verified directly (not just trusting the coder's report)
All 25 CPUID bits against the §1.2 table — every one matches.
Whole-level logic — v2⊆v3, XCR0 gates the whole v3 tier, no headline shortcuts.
The force-down-only property is structural — effective = allowed_level AND isPresent(actual), so it genuinely cannot fabricate capability. Your "fix not a force" principle holds by construction.
The clamp tests — exhaustive nine-combination matrix, pure functions, no synthetic seam in production.
The G3 comptime cross-check — and it exceeds scope: two independent checks (membership deltas and exact captured model sets), fail-closed on any unclassified feature.
C-3 gating, one-way dependency, summary format, reason rule, config-declarations-only — all correct.
Three things done better than required (§3 of the review)

The whole-model-set drift pin, dual markers per gated module (string + code immediate + retained probe export for a runtime-free positive control), and the object-mode semantic root so the detection object emits fully for the disassembly proof.

The judgment calls I made for the new coder's benefit
The dll_probe.zig LF anomaly — I checked the repo: the original file is LF, so preserving LF is correct per C-DELIV-04. I flagged it explicitly as "do not fix this," because a reflexive coder would "correct" it to CRLF and introduce noise.
The asm [_] operand form — flagged as the one genuinely unverified thing (the scope told the coder to prove it by compilation, which never happened). I pointed the successor to close it first via the baseline detection-object disassembly, which settles both asm-correctness and §7.2 at once.
The R3 membership approval — the classification is embodied in code and reads correct to me, but formal W3X/W3D sign-off is still owed before commit; I kept that as a live checklist item rather than letting it pass silently.

Section E gives the successor an ordered action list (verify asm -> run the batch -> produce the @compileError-fires evidence -> bring the classification for approval), and the review is explicit that it is not acceptance — acceptance is W3X building/running and me reviewing that evidence. When that evidence comes back, bring it here and I'll do the acceptance review.

Deblock4 stage 1b3 w3d delivery review v1 0
