# Deblock4 - Stage 5C - W3C Delivery Manifest v1.2

**Correction:** W3D-5C-F2 build-wiring remedy only. This reissue adds the test-only `src/classic_5c_frozen_reexport.zig` single-module shim and changes only the Stage 5C leg-2 module wiring in `build.zig` plus the corresponding imports in `tests/classic_vector_backend_5c_tests.zig`. The shim re-exports the byte-frozen Classic vector backend, scalar kernel, and edge schedule from one `src/`-rooted Zig module so those files are not double-rooted. No production source, frozen Classic file, `build_5C_v1.bat`, or W3D-owned harness is changed or reshipped. W3X should copy these three `apply_to_tree/` files over the already-applied Stage 5C tree and rerun the full matrix with the same W3D runner paths; no production-tree backout is required. W3C has performed static inspection only and makes no build/test PASS claim.
