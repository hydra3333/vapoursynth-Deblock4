# Deblock4 - Stage 5C - W3C Delivery Manifest v1.1

**Correction basis:** W3D blocking finding `W3D-5C-F1` on W3C delivery v1.0, W3X-approved directed remedy. **Delivery form:** correction-only reissue against the already-applied and otherwise-correct `0.1.0-dev+5C` tree; manually replace repository-root `build_5C_v1.bat` with `apply_to_tree/build_5C_v1.bat`. The ONLY change from delivery v1.0 is deletion of the obsolete executed `n02b` loop in `:run_validation_error_cases`, because explicit `x86_64_v3_with_avx2` is now an implemented/accepted Classic backend under S5C-6; nothing replaces that live refusal gate. No `src/` or `tests/` file is reshipped or changed and no backout occurs. W3C static inspection confirms the deleted loop is the only batch delta, the label/call/goto graph remains complete, executable PowerShell content is unchanged, there is no git executable call, and the corrected batch is US-ASCII with CRLF. W3C has not executed the build/proof matrix and makes no PASS claim; W3X re-runs the full matrix with the same W3D runner paths and W3D reviews the evidence.
